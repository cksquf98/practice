#include<iostream>
using namespace std;
int main(int argc, char const *argv[])
{
    cin.tie(0);
    cout.tie(0);

    unsigned long bottom_N;
    cin >> bottom_N;

    unsigned long roundLength = bottom_N*4;
    cout << roundLength;
    return 0;
}